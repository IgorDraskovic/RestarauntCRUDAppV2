<h2>Create a new Order</h2>

<ul>
<%= form_for @order do |f| %>
  <%= f.label :number %>
  <%= f.number_field :number %>
  <%= f.label :food %>
  <%= f.collection_select(:food_id, Food.all, :id, :name) %>
  <%= f.label :party %>
  <%= f.collection_select(:party_id, Party.all, :id, :number) %>
  <%= f.submit %>
<% end %>
</ul>
