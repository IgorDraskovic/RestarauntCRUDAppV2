<h2>Create a new Order</h2>

<!-- Ok but you really need selectors because this way you need to select parties and foods by id number
and if you select the wrong one it will break.  You need a drop down menu selectors of all of your foods and all of your
parties
-->

<%= form_for @order do |f| %>
<%= f.label :fulfilled %>
<%= f.number_field :is_fulfilled %>
<%= f.label :party_id %>
<%= f.number_field :party_id %>
<%= f.label :food_id %>
<%= f.number_field :food_id %>

<%= f.submit %>
<% end %>
